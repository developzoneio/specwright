# Plan - PORT-BROKEN-016

## Phased overview

- Foundation: T01-T04 (seeded `Pattern refs` defects, one per task)
- Behavior: T05-T06 (seeded `Acceptance` / licensed-deviation defects)

## Sequencing rationale

Each task is independent by construction - this spec never executes, it exists to exercise
`/sd:spec validate`'s `SL061`-`SL066` band with one isolated seed per task.

## Risks

None - fixture only.
