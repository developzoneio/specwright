# Plan - FEAT-e2e-resume-demo

Seeded plan for tests/e2e scenario 08. Gate 2 already passed (status `in-progress`).

## Approach

Add two read-only query methods to `TodoService`, each derived from `listTodos()`, each with its
own test in `tests/todo-service.test.js`. `T01` has already been executed and checked off; `T02`
remains.
