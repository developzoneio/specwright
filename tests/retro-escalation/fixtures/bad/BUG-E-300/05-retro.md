# Retro - BUG-E-300

Each line below breaks the logging contract in one way. The validator must reject every one.

escalation: sd-debugger sonnet -> opus (trigger: ESC-BUG-09)
escalation: sd-debugger sonnet -> claude-opus-4-1 (trigger: ESC-BUG-03)
escalation: sd-code-explorer haiku -> opus (trigger: ESC-FEAT-02)
escalation: sd-implementer sonnet -> opus (trigger: ESC-BUG-03b)
escalation: sd-code-explorer haiku -> opus (trigger: ESC-FEAT-02) capped
escalation: sd-spec-architect haiku -> sonnet (trigger: ESC-FEAT-03)
escalation: sd-debugger sonnet -> opus (trigger: ESC-RCA-02) skipped
escalation: sd-debugger sonnet → opus (trigger: ESC-RCA-02)
